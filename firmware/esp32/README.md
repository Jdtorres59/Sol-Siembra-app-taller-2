# Firmware ESP32

Espacio reservado para el sketch / proyecto que controla sensores de humedad, bombas y el techo retráctil.

## Próximos pasos sugeridos

1. Definir la herramienta (PlatformIO, Arduino CLI, ESP-IDF).
2. Documentar requisitos de hardware (pines usados, sensores, actuadores).
3. Agregar archivos fuente en `src/` y encabezados en `include/`.
4. Preparar scripts de build / upload para facilitar las pruebas desde este repositorio.

Por ahora solo se creó la estructura; se añadirá el código definitivo más adelante.
